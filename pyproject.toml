[project]
name = "wikiword"
version = "0.1.0"
description = "Etymology breakdown tool: grounded morpheme decomposition with citations"
requires-python = ">=3.12"
dependencies = ["fastapi", "uvicorn[standard]", "anthropic"]

[dependency-groups]
dev = ["pytest", "httpx"]

[tool.pytest.ini_options]
testpaths = ["tests"]
